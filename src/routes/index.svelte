<main>
    <div class="text-3xl font-bold underline">Web</div>
</main>

<script lang="ts">
    /*import { HubConnectionBuilder } from '@microsoft/signalr';

    const connection = new HubConnectionBuilder()
        .withUrl('http://localhost:40101/class-session/websocket') //https://cotyhamilton.com/sveltekit-environment-variables/
        .build();

    connection.onclose(async () => {
        await start();
    });

    connection.on('addEventAsync', function (eventId: string, timestamp: bigint, eventData: string) {
        alert(eventId + '-' + timestamp + '-' + eventData);
    });

    async function test() {
        await connection.start();
    }

    (async () => await test())();

    setInterval(async function () {
        await connection.invoke('ping');

    }, 45000);*/
</script>

<style lang="scss">
  /*$font-stack: Helvetica, sans-serif;
  $primary-color: #333;

  body {
    font: 100% $font-stack;
    color: $primary-color;
  }*/
</style>