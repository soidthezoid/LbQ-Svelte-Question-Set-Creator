<Button class={`qsc-answer-button settings float-left mb-1 leading-8 bg-settings bg-lime-500 ${$$props.class}`} title="Case sensitivity &amp; whitespace settings" name="eval" on:click /> <!-- NOTE: on:click (no handler) means click is forwarded to parent -->    

<script lang="ts">

    import Button from "../Button.svelte";

</script>